# Attachment Janitor — 개발 메모

이 파일은 이 저장소에서 작업할 때 필요한 배경이다. 사용자용 설명은 [README.md](README.md),
환경 실측과 기획서와 달라진 판단은 [docs/00-환경실측.md](docs/00-환경실측.md)에 있다.

## 목표

"첨부 용량이 어디로 갔나"에 답하는 재료를 모아 보여준다.
**바꿀 수 있는 것은 첨부의 구버전 삭제 하나뿐이다**(v1.1.0). 그 밖에는 조회 전용이다.
삭제 기능을 넣지 않는 것은 의도다 — 형제 앱 Custom Field Janitor 와 같은 원칙이고,
앱 이름·메뉴 라벨에 "Cleanup"/"Delete"를 쓰지 않는 이유도 같다.

기획서 11장의 2~8단계가 들어 있다. 화면 넷(스페이스 랭킹 · 스페이스 상세 · 중복 · 설정)과
사용 설명서, 한영 토글(`?lang=ko|en`), 참조 스캐너, 라벨 5종, 배지 4종, 필터, CSV.
그 위에 v1.1.0 에서 **구버전 정리 하나**가 얹혔다. 그 밖에는 여전히 조회 전용이다.

## 로드맵 (2026-09-14 결정)

순서는 **신뢰 → 규모 → 완결성 → 변경**이다. 앞을 건너뛰고 뒤로 가지 않는다.

1. ~~**7.12.3 에서 실제로 돌려 보기**~~ — **끝**(2026-09-14).
   [docs/01-버전대조-7.8.1-vs-7.12.3.md](docs/01-버전대조-7.8.1-vs-7.12.3.md).
   22항목 중 20 같음 · 1 측정 불가(웹수도, AMPS 환경 차이) · 1 픽스처 이력 차이.
   라벨 13/13 일치, 회귀 없음, 코드 수정 없음. **Postgres × 7.12.3 조합은 여전히 미확인.**
2. ~~**규모 측정**~~ — **끝**(2026-09-14, 실측 24~26).
   본문 51 → 5,131 에 최고 힙 +328 → +393MB(누수 0). **메모리는 문제가 아니라서
   트랜잭션을 쪼개지 않는다.** 비용은 본문 개수에 정비례하고(하나당 45~62ms) 본문 단계가
   전체의 93% 다. 진행률·취소가 본문 단계에서 무의미했던 것과 중복 화면 N+1 도 고쳤다.
   **첨부 축(2,546개까지만 봄)은 여전히 미측정.**
3. ~~**보고 완결성**~~ — **끝**(2026-09-14, 실측 27~29·32). 셋 중 하나만 코드였다.
   - **스페이스 로고**: 코드. 앱이 로고를 [고아] 라고 부르고 있었다 → `RefKind.SPACE_LOGO`.
   - **템플릿**: 실측으로 닫음. 템플릿에는 컨테이너가 없어 `ri:attachment` 를 가질 수
     없다 — 완전히 해석 가능한 참조도 `ri:url` 이 된다. **스캔해도 0 건이다.**
   - **삭제분**: 한계 유지. 9건 중 대부분이 휴지통 페이지 아래고, 첨부를 통째로 지워도
     구버전 행·파일은 남아서 머리만 세면 36% 를 놓친다.
4. ~~**DC 클러스터 잠금**~~ — **구현 끝**(2026-09-14, 실측 35번). `WorkLock` 하나가
   스캔과 구버전 정리를 함께 배타로 만든다. **동작 변화 하나**: 전에는 정리만 스캔 중에
   409 였는데 이제 **스캔도 정리 중에 409** 다.
   단일 노드 실측은 전부 통과했다(동시 스캔 202/409/409 · 정리 도는 중 스캔 409 ·
   그때 진행률이 RUNNING 으로 안 넘어감 · DONE·CANCELLED·정리 뒤 모두 잠금 풀림).
   **노드 두 대의 실제 경합은 검증하지 못했다** — 여기에 클러스터가 없다.
   등급은 예고한 대로 **코드 + 단일 노드 확인 + 클러스터 미검증**이다.
5. ~~**변경 동작 — 구버전 정리 하나만**~~ — **구현 끝**(2026-09-14, v1.1.0, 실측 33번).
   설계는 아래 그대로 지켰다. 대량 실행 실측: 첨부 1,020개 · 버전 1,035개 · 19.4MB 회수 ·
   107초 · 건너뜀 0 · 실패 0, 디스크 51MB → 29MB.
   **로드맵 다섯 단계가 모두 끝났다.**

### 구버전 정리 — 확정된 설계

- **자리**: 스페이스 상세 화면의 파일 목록.
- **행마다 삭제 단추를 두지 않는다.** 체크박스 + **삭제 단추 하나**. 이유 셋:
  행별 단추는 클릭 한 번이 곧 영구 삭제라 오클릭이 바로 파괴이고, 한 건 지우기는
  Confluence 가 확인 화면까지 붙여 이미 제공하며(실측 30번), 이 앱의 가치는 일괄이다.
- **체크박스는 구버전이 1개 이상인 행에만 둔다** — 비활성이 아니라 아예 없음.
  **라벨로 가리지 않는다**: 구버전 삭제는 `ri:attachment` 를 절대 깨뜨릴 수 없으므로
  (실측 31번) [활성]이든 [고아]든 안전이 같다. 라벨로 가리면 없는 규칙을 UI 에 새긴다.
- 표 머리에 **"구버전 있는 것 전체 선택"** 하나. 선택 범위는 **현재 필터에 걸린 전체**
  이지 보고 있는 쪽이 아니다. 고른 개수를 미리보기에 그대로 적는다.
- **"구버전 전체 삭제"를 만들지 않는다. "최근 N개 유지"만 둔다. 기본 N = 3.**
  N 은 살아남는 총 버전 수(현재 포함)다. 페이지가 깨지느냐와 무관하고 **되돌릴 여지를
  얼마나 남기는가**만 조절한다. N=1 도 고를 수 있지만 기본값은 아니다.
- **미리보기(드라이런)를 거치지 않고 실행하지 않는다.** "파일 12개 · 버전 340개 ·
  4.2GB 회수, 각 파일 최근 3개 유지" + 파일별 내역.
- **실행 시점에 각 첨부의 버전 수를 다시 읽는다.** 미리보기와 다르면 멈추고 다시 보여준다.
- 표가 `stale` 이면 실행 차단. 웹수도. `AjActionLog`.
- **체크박스 조건은 "구버전이 있나"가 아니라 "지금 유지 개수로 지울 것이 있나"다**
  (실측 34번). `removableAt = oldVersionCount - (keep - 1)`. 이 식이 `VersionPlan` 과
  어긋나면 누를 수 있는데 아무 일도 안 일어나는 단추가 생긴다 — `RemovableAtTest` 가
  전 조합을 대조한다.
- **정리한 뒤 표를 화면이 직접 깎는다.** `POST /cleanup` 이 파일별 내역을 돌려주고
  화면이 그만큼 뺀다. **서버가 저장된 스캔을 고쳐 쓰지는 않는다** — 스캔한 적 없는
  상태를 스캔 결과처럼 보여주게 된다. 새 스캔이 오면 그 기억을 버린다.
- **정리 관련 알림은 단추 옆에도 낸다.** 위쪽 배너만 쓰면 표를 보고 있는 사람에게는
  화면 밖이라 "눌렀는데 아무 일도 없다"가 된다(실측 34번).
- **한계**: 버전 고정 URL(`?version=3`)로 링크한 곳은 깨지고 감지할 수 없다(실측 28·31번).

**하지 않기로 한 것** (되살리려면 사용자와 다시 정한다):

- **고아 삭제.** 지금 [고아] 는 과다 집계 쪽으로 기울어 있다(템플릿 미스캔 · 이력 기본
  꺼짐 · 삭제분 미집계). 앱이 스스로 치우쳤다고 적어 둔 라벨로 파일을 지우지 않는다.
  3번을 닫으면 다시 꺼내 볼 수 있다.
- **이동.** 용량이 안 줄고, `ri:page` 없는 `ri:attachment` 는 "이 페이지의 첨부"라서
  옮기면 참조하던 본문이 전부 깨진다. 안 깨지게 하려면 본문 일괄 수정 도구가 된다.
- **예약 스캔 + 임계 초과 알림.** 계획서 10장 v2 후보였지만 뺀다.
- **스페이스 관리자용 축소 화면.** 마찬가지로 뺀다.

**형제 앱 customField_janitor 도 조회 전용을 유지한다** — 여기서 구버전 정리를 열어도
저기에는 변경 기능을 넣지 않는다.

## 지금 어디까지 왔나 (2026-09-14 세션 인계)

**커밋 상태.** 2026-09-14 에 **v1.1.0 을 원격 `main` 에 올렸다** — 7.12.3 프로필 · 규모
측정 · 로고 참조 · 페이지네이션 · 구버전 정리 · DC 클러스터 잠금(36번 수정 포함) · 툴팁 ·
번역 교정 · 실측 19~36 이 한 커밋이다. 그 전에는 v1.0.0 초기 커밋(`7177b77`) 하나였다.
**지시 없이 커밋·푸시하지 않는다.** 커밋할 때는 보안 스윕을 먼저 돈다 —
상위 `~/dev/CLAUDE.md` 에 적힌 금지어(계정명 · 비밀번호 · PC 경로 · 개인 식별어 · 시험
인스턴스 포트)를 저장소 전체에서 grep 해 0건인지 본다. 금지어 자체를 이 파일에 적지
않는다 — 적으면 스윕이 이 파일에 걸린다.

**로드맵.** **다섯 단계 모두 끝났다**(1·2·3·5 는 2026-09-14 오전, 4 는 같은 날 오후).
4번(DC 클러스터 잠금)의 등급은 예고한 대로 **코드 + 단일 노드 확인 + 클러스터 미검증**
이다 — 여기에 노드가 하나뿐이라 노드 두 대의 실제 경합은 못 봤다. **커밋 전 리뷰에서
DC 전용 결함 하나를 코드 읽기로 잡아 고쳤다(실측 36번)** — 단일 노드 시험이 못 잡는
종류였다. 그래서 클러스터 미검증이라는 말의 무게가 전보다 크다. **DC 에 실제로 올릴지는
사용자 판단이다**(권한 것은 2노드에서 한 번 볼 때까지 미루는 쪽).

**사용자가 브라우저로 확인해 준 것**: 페이지네이션 · 막대 범례 · `?` 툴팁 · 정리 흐름
(체크박스→미리보기→삭제). **확인해 달라고 했지만 답을 못 받은 것**: 정리 직후 그 행의
구버전 숫자가 바로 줄어드는지, 유지 개수를 3↔2 로 오갈 때 체크박스가 나타났다 사라지는지
(실측 34번 수정분). **절반은 사용자가 보낸 화면으로 이미 확인됐다** — 구버전 2개짜리
`cleanme.bin`·`bugcheck.bin` 에는 유지 3 에서 체크박스가 없고, 6개짜리 `한글 이름.png`
에는 있다. **남은 것은 3↔2 로 오갈 때 나타났다 사라지는지 하나뿐이다.** docker 7.8.1 에
완료된 스캔을 남겨 뒀으니 그 화면에서 유지 개수만 2 로 내려 보면 된다.

**사용자가 직접 잡은 버그** — 실측 34번. 정리한 파일에 체크박스가 남고 미리보기 단추가
먹통이었다. 원인 셋(표가 저장된 스캔이라 거짓말 · 유지 개수 미반영 · 안내가 화면 밖).
고쳤고 `RemovableAtTest` 가 전 조합을 대조한다.

**시험 인스턴스(docker 7.8.1)에 남겨 둔 것** — 지우지 말 것, 다음 단계에서 쓴다:
- `AJC` 스페이스: 페이지 500 × 버전 10 (규모 픽스처)
- `AJB`: 첨부 2,003개, **구버전은 정리해서 0** (`bulk-*.bin` 1,020개에 keep=1 실행)
- `AJA/spec` 페이지에 프로브 첨부 `cleanme.bin`(3버전) · `bugcheck.bin`(3버전) ·
  `probe.bin`(통째로 삭제됨 — 실측 32번의 잔여 표본)
- `AJA` 스페이스 로고(파일명 `AJA`, SPACEDESCRIPTION 컨테이너)
- 전역 템플릿 `AJ Qualified Template` — `ri:url` 로 저장되는 증거 표본
- AO `ACTION_LOG` 에 정리 기록 **배치 6개**. 뒤의 둘(250파일 · 120파일)은 잠금 시험용이라
  **이미 없앤 페이지**(`AJB/locktest`, 통째로 purge)를 가리킨다. 그 둘이
  `ACTION_LOG_PAGE=200` 을 채워서 **`/cleanup/log` 에는 그것만 보인다** — 앞의 넷은
  DB 에 있는데 화면에서 밀렸다(실측 35번 부수 관찰). 스페이스 수치는 정상이다:
  AJB 2,003 · AJA 14 · AJC 500

**7.12.3 을 다시 띄우려면**: `sleep infinity | atlas-mvn -Pconf7123 confluence:run`
(온라인 필요, `-o` 는 synchrony-proxy 미보유로 실패). 지금은 내려 있다. 주소는 AMPS
기본값(`.../confluence` 컨텍스트, 1990 포트)이고 계정도 AMPS 기본 관리자다 — 세 환경변수에
그 값을 넣고 `make-testdata.py` 를 돌린다. `deploy.sh` 는 쓰지 말 것.

**형제 앱 customField_janitor 는 변경 기능을 넣지 않기로 했다**(사용자 결정).

## 환경

- 빌드: Atlassian Plugin SDK (`atlas-mvn`), JDK 8
- 컴파일 대상: confluence **7.8.1** (운영 타깃은 7.12.3). 낮게 컴파일해 높게 돌린다.
- 테스트 인스턴스: docker Confluence 7.8.1 + PostgreSQL
- 오프라인 빌드 가능(`atlas-mvn -o`). 단 최초 1회는 온라인이 필요하다.

**인스턴스 주소·계정은 저장소에 적지 않는다.** 아래 세 환경변수로 넘긴다.
스크립트도 전부 이 규약을 따른다.

```bash
export CONFLUENCE_BASE=http://<host>:<port>
export CONFLUENCE_USER=<admin>
export CONFLUENCE_PASS='...'
```

DB·로그를 직접 볼 때는 컨테이너 이름만 자기 환경 값으로 바꿔 쓴다:

```bash
docker exec <db-container> psql -U <user> -d confluence -c "SELECT COUNT(*) FROM content;"
docker exec <confluence-container> \
    tail -100 /var/atlassian/application-data/confluence/logs/atlassian-confluence.log
```

## 구조

```
co.bskim.confluence.attachjanitor
├── ao/        AjScanRun · AjSpaceStat · AjAttachment · AjRefHit · AjDupGroup
├── model/     Label · Badge · RefKind (열거형이 판정·화면·설명서의 공통 어휘다)
│              AttachmentFacts · RefSource · AttachmentRef · ScanOutcome · ScanProgress
├── analyze/   ReferenceScanner(본문→참조) · HtmlEntities · ReferenceIndex(참조→첨부)
│              Judge(라벨·배지 판정이 있는 유일한 곳) · DuplicateFinder(2단계)
├── scan/      ScanService(5단계 · 잠금 · 실패 보관) · BodyWalker(본문 순회)
├── store/     ScanStore (AO를 만지는 유일한 곳 · 보관 정책)
├── lock/      WorkLock (스캔과 정리를 함께 배타로 만드는 클러스터 잠금 하나)
├── settings/  Settings · SettingsStore (Bandana)
├── rest/      JanitorResource · Json · JsonReader
├── servlet/   JanitorServlet(라우팅·껍데기·웹수도) · HelpPage(설명서)
└── web/       AccessGuard · LocaleText(+Factory) · Screen · StaticAssets
```

새 라벨/배지/참조 종류를 추가하려면 **열거형에 값 하나 + i18n 키**가 전부다. 설명서 표,
화면 필터, CSV 가 전부 열거형을 순회한다. `BundleKeysTest` 가 키 누락을 빌드에서 잡는다.
필터 목록은 서버가 `data-labels` · `data-badges` 로 실어 보내고 JS 가 그대로 쓴다 —
**JS 에 코드를 다시 적지 않는다.** 적으면 열거형에 값을 더해도 필터에서만 조용히 빠지고,
그건 테스트가 못 잡는다.

## 절대 바꾸지 말 것 (기획서와 실측의 근거)

1. **plugin key `co.bskim.confluence.attachment-janitor` 는 바꾸지 않는다.**
   표시 이름은 언제든 바꿀 수 있다.
2. **스캔은 인스턴스 1회 순회다.** `AttachmentDao` **인터페이스**에는 스페이스 단위
   조회가 없다(실측 V3). 스페이스별 버튼을 만들면 같은 전체 스트림을 돌고 대부분을
   버리게 된다. 단 Hibernate 구현체에는 `findLatestVersionIdsIterator(List<Space>)` 가
   있다(실측 8번) — 인터페이스 밖이라 v1 에서는 쓰지 않지만, 대형 인스턴스에서
   트랜잭션을 쪼개야 할 때 이 사실이 출발점이다.
3. **최신 버전 판정은 `PREVVER IS NULL` 이다.** 구버전 행이 *머리* 를 가리킨다(실측 V1).
   체인을 거슬러 올라가는 코드를 쓰면 조용히 틀린다.
4. **구버전 조회는 `getVersion() > 1` 일 때만 한다.** 전 첨부에 부르면 첨부 수만큼
   쿼리가 늘어난다. 다만 **이 필터가 이 앱의 대상 인스턴스에서는 잘 걸러 주지 않는다** —
   호출 1회가 약 14ms 라서, 같은 2,044개 첨부가 구버전 17개일 때 1.2초, 1,048개일 때
   16~20초였다(실측 4번). "스캔은 빠르다"고 적지 말 것. 개선하려면 4·8번을 함께 읽는다.
5. **파일시스템을 읽지 않는다.** DB 메타(`FILESIZE`)가 디스크 크기와 일치한다(실측 V2).
   권한·경로·DC 공유 홈 문제를 통째로 피하는 결정이다.
6. **셀 수 없는 것을 칸으로 만들지 않는다.** "삭제됐지만 디스크에 남은 첨부"는
   `findLatestVersionsIterator()` 가 돌려주지 않아 정직하게 셀 수 없다(실측 2번).
   구조적으로 항상 0 인 칸은 "없다"로 읽히므로 아예 빼고 한계 문구로 적었다.
7. **실패를 조용히 누락시키지 않는다.** 읽지 못한 첨부는 `skippedCount` 로 세어 화면
   배너로 낸다. 실패·취소도 행으로 저장한다. 관리자가 몇 주 전 스냅샷을 방금 것으로
   믿게 만드는 것이 이 도구에서 가장 나쁜 실패이므로, 표에는 늘 기준 시각이 붙는다.
8. **클라이언트도 조용히 실패하지 않는다.** XHR 이 401/403 이면 폴링을 멈추고 버튼을
   되살리고 이유를 낸다. 그래서 REST 에는 웹수도를 걸지 않는다(아래).
9. **판정 규칙은 `Judge` 한 곳에만 있다.** 화면·CSV·설명서가 같은 열거형과 같은 판정을
    보아야 설명서와 실제 동작이 어긋나지 않는다.
10. **참조 스캐너는 매크로를 열거하지 않는다.** `ri:attachment` 엘리먼트를 트리 어디서든
    전부 걷는다(실측 V5). 예외는 평문 파라미터를 쓰는 `gallery` 하나뿐이다.
    매크로 목록을 다시 만들지 말 것 — 목록은 빠뜨리는 쪽으로 틀린다.
11. **본문 형식이 둘이다.** 페이지·블로그·댓글은 XHTML, **스페이스 설명은 wiki**
    (실측 10번). 하나의 파서에 넣으면 스페이스 설명이 전부 분석 실패로 잡힌다.
12. **스페이스 로고는 참조를 합성해 붙인다.** 로고는 SPACEDESCRIPTION 컨테이너의 첨부인데
    어느 본문도 가리키지 않아서, 그냥 두면 앱이 스페이스 로고를 [고아] 라고 부른다
    (실측 27번). 파일명은 `SpaceLogo#getDownloadPath()` 에서 뗀다 — **추측하지 않는다.**
    같은 컨테이너의 다른 파일은 그대로 [고아] 다.
13. **배너와 표는 서로 다른 실행을 본다.** 배너는 `latestRun()`(상태 불문), 표는
    `latestCompleteRun()` 이다. 스캔이 실패했다고 마지막으로 성공한 표를 치워 버리면
    관리자는 아무것도 못 보고, 그렇다고 날짜 없이 옛 표만 두면 방금 것으로 읽는다.
    둘을 함께 내고 다르면 `aj.state.stale` 로 다르다고 적는다. 취소된 실행도 저장한다 —
    흔적이 없으면 취소가 없었던 것처럼 보인다.

## 함정 (직접 밟고 고친 것들)

전부 [docs/00-환경실측.md](docs/00-환경실측.md) 에 근거와 함께 있다. 요약만:

- **백그라운드 스레드의 AO 쓰기도 SAL `TransactionTemplate` 으로 감싸야 한다.**
  Confluence 의 AO 는 호스트 Hibernate 세션 위에서 돈다. 감싸지 않으면
  `No Hibernate Session bound to thread` 다. 읽기와 쓰기는 별도 트랜잭션으로 나눈다 —
  하나로 묶으면 대형 인스턴스에서 스캔 내내 트랜잭션이 열려 있다(실측 1번).
- **SAL `@WebSudoRequired` 는 Confluence 플러그인 서블릿에 아무 효과가 없다.**
  애노테이션만 붙인 상태에서 로그인만 한 세션으로 화면이 그대로 열렸다.
  `WebSudoManager` 를 서블릿에서 직접 부른다(실측 3번).
  **REST 에는 걸지 않는다** — 스캔이 웹수도 제한 시간보다 오래 걸리면 폴링이 도중에
  끊기고, 관리자는 "눌렀는데 버튼이 죽었다"만 본다.
- **`catch (RuntimeException)` 으로는 부족하다.** 7.8.1 로 컴파일해 7.12.3 에서 돌리므로
  현실적인 실패는 `NoSuchMethodError` / `NoClassDefFoundError` — 전부 `Error` 다.
  스캔 경로는 `Throwable` 을 잡는다. 안 잡으면 진행률이 RUNNING 에 박혀 화면이 영구
  폴링한다.
- **i18n `.properties` 는 ISO-8859-1 로 읽힌다.** 한글은 `\uXXXX` 로 escape 해야 한다.
  `i18n/*.properties.src`(UTF-8)를 고치고 `tools/make-i18n.py` 로 생성한다.
  `src/main/resources/attachment-janitor*.properties` 를 직접 고치지 말 것.
  `build.sh` / `deploy.sh` 가 빌드 전에 자동으로 다시 만든다.
- **plugin-icon/logo 는 PNG 다.** SVG 는 UPM 의 ImageIO 가 디코드하지 못해 빈칸이 된다
  (easy-numbering 에서 겪은 것). `images/plugin.png` 가 원본이고 `tools/make-icons.py` 가
  72·144 두 크기를 만든다. **원본을 바꿨으면 이걸 다시 돌려야 한다**(실측 22번).
- **관리 화면 제목을 본문에 찍지 않는다.** `atl.admin` 장식이 `<title>` 을 제목으로 이미
  그린다. `<h1>` 을 더하면 화면 맨 위에 제목이 두 번 나온다(실측 21번).
- **표는 쪽으로 끊는다.** 한 장에 다 그리면 실제 인스턴스에서 수천 줄이다. 쪽당
  10/20/50/100(기본 50), 이동 줄은 `53건 · ‹ 1 2 3 … 6 ›`. **자르기는 정렬 뒤에**
  하고(자르고 정렬하면 쪽마다 순서가 달라진다), 필터가 바뀌면 **1쪽으로 되돌린다**
  (3쪽을 보다 조건을 좁히면 빈 화면이 된다). 쪽 번호 창 규칙은 `pageNumbers` 에 있다.
- **진행률은 총계를 모를 때 비율을 지어내지 않는다.** 본문 단계는 분모가 스페이스 수라
  실제 진척과 안 맞는다. `percent()` 가 -1 을 내고 화면이 흐르는 막대를 그린다. 대신
  읽은 본문 수를 계속 올린다. **취소·실패에는 100% 를 찍지 않는다** — 끝난 것과 완주한
  것은 다르다(실측 25번).
- **오래 도는 루프 안에서 취소를 확인한다.** 스페이스 사이에서만 보면 페이지가 많은
  스페이스 하나가 취소를 5분 지연시킨다. `BodyWalker.Watcher` · `DuplicateFinder.Watcher`
  가 그 통로다. 새 루프를 만들면 여기에도 붙일 것(실측 25번).
- **칸이 두 수치를 보이면 툴팁도 둘을 설명해야 한다.** `구버전`·`참조 없음` 칸은
  `용량 · 개수` 인데 툴팁이 용량만 말하고 있었다(사용자 제보). 표시를 바꿀 때 툴팁을
  같이 안 고치면 앱이 자기 화면을 반만 설명한다.
- **클러스터 잠금은 잡은 스레드가 놓아야 한다.** 두 호스트 구현이 다 소유자를 따진다.
  스캔은 HTTP 스레드가 시작하고 몸통은 워커 스레드에 있어서, 요청 스레드에서 잡으면 놓을
  곳이 없다 — **워커 안에서 잡고** 요청 스레드는 래치로 결과만 받는다. 그리고 **잠금을
  잡은 뒤에만 진행률을 RUNNING 으로 세운다.** 순서를 뒤집으면 시작하지도 않은 스캔을
  화면이 영원히 폴링한다(실측 35번).
- **`ClusterLock` 에서 `tryLock()`·`unlock()` 외의 메서드를 부르지 말 것.** DC 구현
  (`HazelcastDualLock`)은 `isHeldByCurrentThread()`·`lockInterruptibly()`·`newCondition()` 을
  전부 `UnsupportedOperationException` 으로 던진다. Server 구현은 답하기 때문에 **단일 노드
  시험은 이 결함을 절대 못 잡는다** — 첫 스캔 뒤 DC 에서 잠금이 영영 안 풀리는 코드가
  여섯 가지 시험을 다 통과했다(실측 36번). 소유자는 `WorkLock` 이 자기 필드로 기억한다.
  `WorkLockTest` 의 Hazelcast 꼴 가짜가 이걸 지킨다 — 가짜를 빼지 말 것.
- **`lock()` 을 쓰지 말 것. `tryLock()` 만.** 요청 스레드가 막혀 기다리면 스캔이 끝날
  때까지 몇 분 동안 버튼이 죽은 것처럼 보인다. 못 잡으면 즉시 409 다.
- **409 에 무엇이 막고 있는지 적지 않는다.** 잠금을 쥔 쪽이 스캔인지 정리인지, 이 노드인지
  다른 노드인지 알 방법이 없다. 없는 정보를 지어내지 않는다.
- **`atlas-run -o` 는 안 된다.** `com.atlassian.synchrony:synchrony-proxy:war:6.0.1` 이
  로컬 저장소에 없어 오프라인 기동이 실패한다. 최초 1회는 온라인이 필요하다.
- **`atlas-run` 인스턴스에 `deploy.sh` 를 쓰지 말 것.** AMPS 가 플러그인을 이미
  `bundled` 로 설치·활성화한다.
- **`generate-rest-docs` 의 "package ... does not exist" 경고 30건은 소음이다.**
  javadoc 골의 클래스패스가 `rest` 패키지로 좁아서 난다. 같은 로그에서 컴파일러는
  "Nothing to compile" 이고 앱은 정상 기동한다. 이걸 컴파일 오류로 읽고 쫓지 말 것.
- **`.mvn/jvm.config` 를 지우지 말 것.** `-Duser.name=BSKIM` 한 줄이 없으면 jar 매니페스트의
  `Built-By` 에 빌드한 사람의 OS 계정명이 실린다.
- **같은 버전을 다시 설치하면 브라우저가 이전 빌드의 JS 를 쓴다.** Confluence 가 웹 리소스
  URL 을 플러그인 버전으로 키잉하고 immutable 로 준다. `deploy.sh` 가 버전에 빌드 시각을
  붙이는 이유다. 이 앱은 JS 를 서블릿이 인라인으로 내보내므로 영향이 작지만, web-resource
  를 추가하는 순간 다시 문제가 된다.
- **본문에 HTML 엔티티가 그대로 있다.** DTD 없는 XML 이라 파서가 첫 `&nbsp;` 에서 죽는다.
  선언이 아니라 치환으로 없앤다(`HtmlEntities`, 실측 11번). 파서는 **네임스페이스
  비인식**으로 돌린다 — `ac:`/`ri:` 프리픽스가 선언 없이 들어 있기 때문이다.
- **`PageManager` 가 `ContentEntityManager` 를 상속한다.** 둘 다 `@ComponentImport` 하면
  `NoUniqueBeanDefinitionException` 으로 앱이 기동하지 않는다(실측 12번).
- **AO 엔티티는 `atlassian-plugin.xml` 의 `<ao>` 에도 등록해야 한다.** 빠뜨리면 컴파일도
  기동도 되고 **스캔 저장 단계에서만** 테이블 없음으로 죽는다(실측 13번).
- **`{0}` 이 있는 문구에 숫자를 넘기면 자릿수 구분이 들어간다**(1095 → `1,095`).
  `MessageFormat` 의 정상 동작이다.
- **스캔 스레드에는 보는 사람의 로케일이 없다.** 그래서 스캔은 사람이 읽는 문장을 저장하지
  않는다. 화면 문구는 전부 서블릿이 요청 시점에 i18n 으로 만들어 JSON 으로 넘긴다.

## 검증 방법

```bash
# 위 세 환경변수가 export 되어 있다고 본다.
python3 tools/make-testdata.py                 # 픽스처 (라벨/배지별로 하나씩)
python3 tools/make-testdata.py --bulk 2000     # 첨부 축 대량 생성 (AJB/bulk)
# 본문 축 대량 생성 (AJC). 페이지 수와 이력 깊이가 따로 놀아야 어디가 비싼지 갈린다.
python3 tools/make-testdata.py --bulk-pages 500 --bulk-versions 10

curl -s -u "$CONFLUENCE_USER:$CONFLUENCE_PASS" -H 'X-Atlassian-Token: no-check' \
     -X POST "$CONFLUENCE_BASE/rest/attachment-janitor/1.0/report/scan"
curl -s -u "$CONFLUENCE_USER:$CONFLUENCE_PASS" \
     "$CONFLUENCE_BASE/rest/attachment-janitor/1.0/report/spaces" | python3 -m json.tool

# 힙 (컨테이너 안에서, 스캔 전후로 강제 GC 를 걸고 비교한다 — 실측 24번)
docker exec -u confluence <confluence-container> jcmd 1 GC.heap_info

# 교차 확인 — 앱 수치와 DB 수치가 같아야 한다 (쿼리는 docs/00-환경실측.md V4)
docker exec <db-container> psql -U <user> -d confluence -c "..."

# 권한: 비관리자는 전부 403, 익명은 401
curl -s -u "<비관리자 계정>:..." -o /dev/null -w '%{http_code}\n' \
     "$CONFLUENCE_BASE/rest/attachment-janitor/1.0/report/spaces"

# 중복 실행 방지: 하나만 202, 나머지는 409
for i in 1 2 3; do (curl -s -o /dev/null -w "$i=%{http_code} " \
  -u "$CONFLUENCE_USER:$CONFLUENCE_PASS" -H 'X-Atlassian-Token: no-check' \
  -X POST "$CONFLUENCE_BASE/rest/attachment-janitor/1.0/report/scan") & done; wait
```

권한 시험에는 관리자 권한이 **없는** 계정 하나가 따로 필요하다. 계정명·비밀번호는
저장소에 적지 않는다.

## 남은 일

- **정리가 요청 스레드에서 몇 분을 돈다.** `MAX_IDS=5000` × 파일당 약 105ms ≈ 9분,
  실측 1,020개는 107초였다. 로드밸런서·프록시의 유휴 시간(보통 60초)을 넘으면 브라우저는
  `aj.error.network` 를 보는데 **삭제는 서버에서 계속된다.** 무결성은 무사하다 —
  `AjActionLog` 는 다 남고 `markStale()` 도 돈다 — 하지만 `done` 응답이 사라져 표가 자기
  수치를 깎지 못한다. 상한을 낮추거나(500?) 정리를 비동기로 바꾸는 것이 답인데
  **사용자 결정 사항**이다(2026-09-14 리뷰에서 발견, 미수정).
- **큰 정리 배치 하나가 `/cleanup/log` 를 독차지한다.** 파일당 한 행 · 페이지 200행이라
  250파일 배치 뒤로 이전 배치가 화면에서 사라진다(실측 35번 부수 관찰). 배치 단위 요약이
  답이다. 미수정.
- 삭제된 첨부의 용량을 정직하게 세는 방법 (실측 2 · 29 · 32번). 휴지통 전체를 주는 API 가
  인터페이스에 없는 것이 막는 지점이다. **세게 되면 머리 행만 세면 안 된다** — 첨부를
  통째로 삭제해도 구버전 행과 파일은 `current` 인 채 그대로 남는다(이 인스턴스 36% 차이).
- **`ri:url` 참조 따라가기.** 템플릿과 붙여넣은 내려받기 링크는 첨부를 URL 로 가리킨다
  (실측 28번). 지금은 따라가지 않는다 — `ri:attachment` 와 달리 Confluence 가 뜻을
  보장하지 않는 문자열이고, 열면 `<a href>` · 썸네일 · 버전 고정 · 컨텍스트 경로를 전부
  다뤄야 한다. 넣는다면 한계와 함께 별도로 논의할 기능이다.
- **Postgres × 7.12.3.** 대조(문서 01)는 H2 축이고, Postgres 축은 7.8.1 에서만 봤다.
  각각 통과한 것이지 교차 확인이 아니다.
- **노드 두 대의 실제 경합.** 잠금은 넣었지만(실측 35번) 여기에는 클러스터가 없어
  교차 JVM 을 증명하지 못했다. `WorkLockTest` 는 노드 대신 스레드 두 개로 본다.
  잠금을 쥔 채 노드가 죽었을 때의 회수도 Confluence 구현에 달렸고 확인하지 못했다.
- **노드가 다르면 진행률이 안 보인다.** A 가 스캔하는 동안 B 에 붙은 화면은 아무것도
  못 본다 — 진행률이 메모리에만 있다. 고치려면 시작 시점에 RUNNING 행을 써야 하는데,
  그러면 노드가 죽었을 때 영영 안 지워지는 RUNNING 행이 배너에 남는다(규칙 13). 안 고쳤다.
- **구버전 조회 비용.** 실측 4번. 지금은 첨부당 1회 쿼리(약 14ms)다. 구버전이 많은
  인스턴스가 바로 이 앱의 대상이므로 여기가 사실상의 성능 상한이다.
- **첨부 축의 대형 인스턴스 메모리.** 실측 8번. `findLatestVersionsIterator()` 는 `Query.iterate()`
  라 읽은 엔티티가 트랜잭션이 끝날 때까지 1차 캐시에 남는다. 스캔 전체가 트랜잭션
  하나이므로 컨텍스트가 첨부 수에 비례해 커진다. **본문 축은 쟀고 문제없었지만(실측
  24번) 첨부 축은 2,546개까지만 봤다.** 바운드가 필요하면 스페이스 단위로 트랜잭션을
  쪼갠다(출발점은 8번).
